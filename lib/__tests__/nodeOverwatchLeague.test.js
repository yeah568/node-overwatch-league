const assert = require('assert');
// Const nodeOverwatchLeague = require('../index.js');

describe('nodeOverwatchLeague', () => {
  it('has a test', () => {
    assert(true, 'nodeOverwatchLeague should have a test');
  });
});
